[project]
name = "lico-config"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["lico-logger"]

[tool.hatch.build.targets.wheel]
packages = ["src/lico_config"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
