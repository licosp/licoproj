[project]
name = "lico-llama"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["huggingface-hub>=1.18.0"]

[project.scripts]
lico-llama-huggingface = "lico_llama.huggingface:main"

[tool.hatch.build.targets.wheel]
packages = ["src/lico_llama"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
