[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lico-devc"
version = "0.0.1"

[project.scripts]
lico-devc = "lico_devc.boot:main"

[tool.hatch.build.targets.wheel]
packages = ["src/lico_devc"]
