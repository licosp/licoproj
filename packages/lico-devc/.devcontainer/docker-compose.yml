services:
  lico-resident:
    build:
      context: ../../..
      dockerfile: packages/lico-devc/.devcontainer/Dockerfile
    volumes:
      - ../../..:/workspace:cached
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "2222:22"
    stdin_open: true
    tty: true
