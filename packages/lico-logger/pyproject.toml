[project]
name = "lico-logger"
version = "0.1.0"
description = "Centralized logging and message constants for licotor"
authors = [{ name = "Antigravity" }]
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
