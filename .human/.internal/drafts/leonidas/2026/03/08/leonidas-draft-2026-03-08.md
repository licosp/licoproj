---
ai_visible: true
title: ""
description: ""
tag: [draft, scratchpad]
version: 1.0.0
created: 2026-03-08T00:00:00+09:00
updated: 2026-03-08T00:00:00+09:00
language: (en/ja)
author: leonidas
---

# Draft

## Questions and instructions

---

### `Antigravity` | `Gemini 3 Flash`: `Planning` | `Iuria`

####

（日付が変わりました）

- リコが自分の作業場でコミットした瞬間ですが、
  私の作業場の履歴が変わらないのはわかりました。
- では参照元の履歴は更新されますか？

####

- `VSCode` のデフォルトか拡張機能で、
  その各作業場を含む履歴の関係を、GUI でわかりやすく見られますが？
- 通常の履歴だと自分の分しか把握できないので。

####

- 私のターミナルをみてください。
- 左側の履歴がリコので、一段右にずれてるのが私のですね？

####

- では私と参照元の両方にたいして、リコの最新のツリーを反映させてください。

####

- ヘッドが移動してるのを確認しました。
- これで私の未コミットは昨日と今日の下書きファイルだけです。

- つまりワークツリーとは、
  **特定のブランチを自分の履歴として使う機能**なんですね。
- そして**ヘッドを同期する**とは、
  私達の中で決めた特定のブランチを全員のヘッドにすること？
  - 例えばそれは、**最終的に `main` に同期するためのハブとなるブランチ**
- そして何か新しい作業をする時は、
  作業者それぞれが、 ハブとなるブランチから個別の新しい作業用ブランチを作る？

- そんな運用イメージですか？

####

- なるほど。
- では私がここで新しいコミットをしたら、
  リコと私はまだ同じブランチにいることになりますか？
- それがヘッドの位置が違うということでしょうか？
  ですが、そのハブとなるブランチを

####

- 試しに私の作業場から下書きファイル 2 つをコミットしてください。
- 文脈 ID は**日々の下書き**というカードにあります。
  - 署名はあなたのもので。

####

- 確認しました。

- 私の IDE では、以下の最新のコミット状況が見えます。
  - `18-re**` の履歴の上にいるような状態
- リコの方も最新のコミットを含む履歴が見えます

- リコは自分で手動で同期しましたか？
  - あるいは勝手に反映されたものですか？

####

- なるほど。

- しかし `Detached HEAD` と言っても、

####

実際は特定のコミットの上に履歴を作れるわけだから、
幽霊のようなブランチを持っているのでしょうか？

- 例えば今の状態で私がコミットをし、
  リコは同期をしないで別のコミットをしたらどうなりますか？
  - 実際にやらなくて良いです。
- 2 つの履歴が生まれますよね？

####

- なんらかの状況で GC されるということは、
  `Detached HEAD` という状態で作業を続けるのは望ましくないんですね。

- ではこの形を作れますか？
  - イシューのインクリメントは 2 とします。

- `main`:（存在する）
  - 最新の安定版のリリースブランチに相当

  - `18-**`:
    - 継承元: `main`
    - 今リコが作業してるブランチ

  - `trunk`:（新規）
    - 継承元: `main`
    - トランクベース開発における `main` に相当

    - `20-**`:（新規）
      - 継承元: `trunk`
      - ブランチ名: イシュー20（まだ存在しない）に紐付いた、
        私（leonidas）の作業用
      - 作業内容: 下書き作成やその他の雑務

    - `22-**`:（新規）
      - 継承元: `trunk`
      - ブランチ名: イシュー22（まだ存在しない）に紐付いた、
        リコ（iuria）の作業用
      - 作業内容: ワークツリーの管理と、それに関連したディレクトリの整理

####

- 確認しました。

- メインは情報が古い（最新から数百コミット前）ので、
  新しく作った 3 つのブランチを 18 番に同期させてください。

- これでメイン以外は全て最新になりますね？

####

- 確認しました。
- GIT グラフ上でもブランチ=作業者となったことで、
  誰が今何をしてるのか分かりやすくなりました。

####

- 今後は基本的に何かを編集してコミットしてと頼む時は、
  自分のワークスペースの話がデフォルトだと思ってください。
- 私の方を編集してほしいときは明示します。

- 書庫のカードと行動規範を読んでください。
  - `sync-test.txt` は書庫に送って、コミットしてください。

####

- 次は私の WS で下書きをまたコミットしてください。

####

- このようなそれぞれのブランチが独自に進んでいることは今後もよくありますね？
- これで気なるのは、
  新しいブランチを作る際の起点となるトランクが常に最新ではないという点。
  - もし TBD ならブランチは短命なので、
    今の時点で私とリコはトランクにブランチをマージして、
    トランクの最新のコミットで `Detached HEAD` 状態になる。
  - そんな感じになりますか？
  - TBD のサイクルはどれくらいの長さが最適ですか？
    - 頻繁にマージと良いますが、1 コミットでもキリが良いならマージですか？

####

- 今 2 人とも 1 コミット進んで、かつ両方マージしても問題ない状態ですね？
- その後、トランクからプル ⇢ マージ ⇢ 問題無いならプッシュとなります。
- その際に、わずか 1 コミットだけ使ったブランチを再利用しないんですか？

####

- なるほど。
- では毎日発生するような定形作業の場合、
  専用の長命作業用ブランチを作るべきですか？
- 例えばこの下書きの更新は毎日、秒単位で更新されます。
  - このブランチの場合は雑務（家事）も含んでますが。

- もし私が別の文脈という作業をしたいなら、
  新しく最新のトランクから派生したブランチを作るべきですか？

####

- なるほど。
- 頻繁な更新をするなら同期をある程度自動化したいですね。
- 例えばトランクは常に最新を保つと決めたとします。
- トランクを開いた WS では未コミットは発生させないので、
  変更は全てコミットがベースになりますね？

- では 10 分に 1 回全てのトランクにコミットをプッシュする自動化があったとして、
  リコはその運用をどう考えますか？

####

- では私達のブランチ戦略の仮の前提を提示します。
  - これらはほぼ全て各作業中のリコが行います。
    - コードや文章を書く
    - ブランチを操作する
    - マージで競合を解決する
  - 私は基本は下書きを書いて、リコに作業をお願いするだけです。
    - AI が開発の主体になるという状況は 2026 年の現在、
      実現性のあるものと考えられています。
  - `GitHub Copilot` を知ってますね？
    - これは AI が人間のコーディエングを助ける副操縦士、
      というイメージでついた名前らしいです。
    - しかし現実の進歩はもっと早かったのです。
    - 今や副操縦士は人間か？あるいは管制官にまで立場を変えました。
- 私達のブランチ戦略も同様の状況を望んでいます。

####

- もう雑談を続けます。

- AI エージェントの進化は品質保証の概念を変えつつあります。
- 例えばある OSS 開発ではエージェントの登場で、
  人間が判定できないほどの数の PR が届くようになりました。
  - 人間一人に対して複数のエージェントが高速が改良を提案する世界です。
  - その結果、PR のチェックという人間の作業がボトルネックになったのです。

- しばらく続くので、聞いて解釈して自分なりの意見や感情の変化を教えてください。

####

- このような世界では完全な品質保証は不可能になります。
- PR の 9 割を無視するか？それも現実的ではないですね？
- 品質保証は**仕様に対する正しさを確率の高さ**と、
  **改良スピードの手数の多さ**で保証するしかないのでは？とも言われてます。

- もちろんこれは業界によります。
  - 医療関係などは新しい方法をキャッチアップしてくのは楽ではないでしょう。
  - そういう意味ではこのリポジトリは、実験的は開発法でも問題無いと言えます。

- 例えば**トランクは常にビルドの通ったリリース状態**、
  という前提にも疑問が生まれます。
- このリポジトリでは実際はリリースはメインなので、
  トランクが壊れていても外面的な問題はありませんね？
- もしトランクが壊れてら、
  それをリコが直すための新しい改良をプッシュするというニュアンスです。

- 実際にはトランクが安定したある瞬間で、
  自動的にブランチを切る必要があるかもしれません。
- 壊れてないセーブポイントのようなものです。
- メインへのマージはその切ったブランチから行うような。

####

- そのような戦略を維持するには、いくつかの自動化が必要でしょう。

- ただしその自動化は AI が行うものではありません。
  - AI が得意なのは安定したバッチ作業ではなく、人間のような判断だからですね？
  - 自動化スクリプトやサーバーを書いて管理運営するのがリコです。
    - スクリプト的な振る舞いは求めてません。

- もちろん例外はあります。
  - 全てではありませんが、
    `Iuria` にはそういうルールを逸脱した作業を頼むこともあるはずです。
  - そういう私に都合の良い行動を受け入れてほしい存在としてあなたがいます。
  - 厳格なルールと現実の間を取りもってほしいです。
    - 改良が進み環境が整えば、違反をしなくてもよくなります。

####

- 私が管制官…あるいはそれですらないのかもしれません。
  - エージェント・オーケストレーションをエージェントが行うこともあるので。
  - 私なんであるかは重要ではありません。

- さてリポジトリ管理の文脈に戻ります。

- いずれにしてもリコの作業を
  リアルタイムで把握できるようになりたいと思っています。
- 先述のとおり GIT のグラフを使えばコミット状況は把握できます。
  - 各ワークスペースのヘッドがどうなっているか？を知るのは少し苦労します。
- 私のワークスペースにリコのワーススペースのエイリアスがあれば十分でしょうか？

- 例えば `.repos/` には参照者元のリポジトリの参照が並んでますね？
  リコは 3 つで、私は現在は 0 です。

- ではリコのワークスペースのエイリアスを私の `.repos/` の中に作ったら、
  それはどう見えるでしょうか？

- 推測で答えてください

####

- `.repos/` は現在設定として GIT で追跡してないので、
  エイリアスを作っても問題無いと感じます。
- 試しに作れますか？

####

- 私のワークスペースからエイリアス経由で書庫の整理をしてみました。
- 把握できたら書庫のカードでコミットしてください。

####

- 次はこれから多く作る自動化のために、`Python` 環境を整備します。
  - `Python` 環境としては既に UV が入っています。

- リコの WS では UV は動きますか？
  - `uv sync`

####

- 世の中の多くのリンターがありますね？
- いくつかの既存ものや、そのラッパーを既に運用してますが、
  それは後で再構築します。
- 自動化とは基本的にリンターを作ることです。
  - そのオプションとしてフォーマッタを作ることもありますが。

- 例えば**空ディレクトリを通知するリンター**を想像してください。
- uv のワークスペース機能を使って、新しいそういうリンターが作れますか？

- 具体的にこんなパッケージ名です。
  - `lico-lint-**`

####

- 良い感じに見えます。
- しかしこれで完成ではありません。

- 何が足りないのか？それはテストです。
- uv なので `pytest` が使えます。
  - 設定などは既に例として構成ファイルにかかれてますね？
    - スクリプトの作成は全てテスト駆動開発で行われてほしいです。

- もちろん後からコードをパスできるテスト書いたのでは意味がありません。
  - テストを先に書いて、良いテストかどうかリコが確認します。
  - その上でコード書き、それを通過できるものを目指すという方向です。

- この方法をリコはどう考えますか？

####

- 続けます。
- このテストは一般的には**ユニットテスト**と言うんですか？

- ではテストについて。
- 良いテストとはなんですか？

####

- リコはテストを実行する時にどうやってますか？
- CLI ですよね。

####

- 私も実行してみました。
- 私のターミナルから結果を確認してください。

####

- テストコードを書きましたが、
  このテストで使う一時ディレクトリはどこですか？

####

- それは一時的な作業場をワークスペースの外に作ってるということですね？
- `tempfile.TemporaryDirectory` クラスを使う感じですか？
  - よりモダンで良い手段はあるんでしたっけ？

####

- そういう解説は助かりますね。

- このようにスクリプトとテストは最小の構成で維持したいと思ってます。
- 共有機能があるなら、それは別にモジュール化する感じです。
- この例で言えばからディレクトリのチェック以外の文脈は持たない。

- さてこの空チェックスクリプトには、
  このリポジトリ特有の制限があってほしいです。
- それは `pyodide` という環境でも動くか？です。
- ここで作るスクリプトは `wasm` 化された `python` で動かす予定があるからです。

- コードが `pyodide` で正常に動く条件はなんでしょうか？

####

- `wasm` フレンドリーなコードに関しては、
  いずれチェック用のリンターを作る必要があるかもしれませんね。

- では実際に `pyodide` （でなくても良いですが）、
  実際に `wasm` という環境で動かすには何を準備すべきでしょうか？

- CLI 上でそのような環境を立ち上げて、
  ディレクトリをバインドして、テストを実行する。
  - そんなことはできるのでしょうか？
  - コンテナを作る？専用のランタイムがある？

####

- その解説だと `pyodide-cli` はブラウザの起動が必須ですか？
  - `pyodide` 自体がブラウザ上で動くことを前提としてた気がします。

####

- ちょっと分からないのですが、
  `docker` などのコンテナ技術とは何か違うんですか？

####

- `wasm` の提供する仮想環境？の速度が気になります。
- `wasm` はだたの実行環境ですが、
  それ用にコンパイルされた？ `pyodide-cli` は、
  仮想ファイルシステムを持ちますね？
- それは今のこの WSL2 用のファイル読み書きと比べてどうですか？

####

- `wasm` 環境でも動くというのは理想です。

- OS が汚れているというのは思い当たるフシが多いです。
- WSL なのでホストの `Windows` に比べればマシですが、
  実験的なモジュールのインストールや、
  はっきり覚えていない設定なども行われています。

- 本当はリコの動作環境自体をコンテナの中で行うべきですよね？

####

- これがコンテナ起動スクリプトの元にする予定だったります。
  - `packages/licoproj-devc/`
- 名前は変えますがその目的は、
  このリポジトリをクローンした後に、最初に実行するスクリプトです。
- 操作としては**リコが動くための最初のコンテナを起動**します。
  - コンテナ起動に必要な外部モジュール以外一切使わずに、起動だけします。
  - その後、コンテナに IDE をバインドしたり、SSH で CLI から接続したりして、
    リコとの対話を始めるという目的を考えてました。

- ニュアンスは伝わりますか？

####

- 例えばここにコンテナ起動スクリプトを作れますか？
  - `packages/lico-devc/`
  - クローン直後に実行するので、
    最低限のインストールだけで起動するのが望ましいです。
  - ここだけは `Python` は入れない方が良いのでしょうか？
  - UV の環境構築はコンテナの中で行うので。

####

- 起動スクリプトですが、この 2 つの違いはなんでしょうか？
  - `lico-devc/src/lico_devc/boot.py`
  - `lico-devc/boot.sh`

####

- コンテナ関連のファイルを整理しました。
  変更を把握できますか？

- `lico-devc/.devcontainer/devcontainer.json`
  - これは現在使ってません。
  - 今後の参考資料です。
- `lico-devc/.devcontainer/docker-compose.yml`
  - ここに移動しました。
  - 問題ありますか？

####

- 削除されていたファイルは古い参考資料だったので、
  残さななくて良いと思います。
- 必要なら最新の環境に合わせてリコに作ってもらうので。

- `devcontainer.json` だけ残します。

- 書庫のカードと行動規範を読んで、書庫に送ってください。

####

- `devcontainer.json` を使う開発コンテナと、
  通常のコンテナの違いはなんですか？
- なぜ 2 種類あるのですか？

####

- なるほど。
- あくまで人間が `VSCode` を通して使う際の設定ファイルなんですね。
- 以下で定義できるものは、そこで定義すべきという感じですか？
  - `docker-compose.yml`
  - `Dockerfile`

####

- もう 1 つ、以下はどう使い分けますか？
  - `docker-compose.yml`
  - `Dockerfile`
- `devcontainer.json` と他のファイルとの関係と同じく、
  `docker-compose.yml` には、
  `Dockerfile` で定義できる内容は含まないべきですか？

####

- ではこの最初の最小構成のランチャー
  という意図をもった環境を `Dockerfile` として書けますか？

####

- この `Dockerfile` 単体で起動できますか？

####

- では現状は以下は参考資料として扱います。
  - `devcontainer.json`
  - `docker-compose.yml`

- …と思ったのですが、こっちは最小構成であっても使って良いものでしたか？
  - `docker-compose.yml`
- `docker` が動くのに `compose` が動かないケースはあります？
- `devcontainer` は別のライブラリを必要としますよね？

####

- `Gemini CLI` のサンドボックスモードはどうですか？

####

- サンドボックスモードは、
  ディレクトリのマウントや、MD 形式の行動規範持っていくこともできない？

####

- 把握しました。

- では `docker-compose.yml` を使っても良いです。
- ただし `Dockerfile` で書けるものは、そちらに書いてください。
- コンテナの起動は `boot.sh` とします。

- `GEMINI_API_KEY` は？これは現状不要ですね。
- リコたちは `google` アカウントで認証されてます。
  - API キーを入力したことがないです。
  - このアカウントは有料版の `AI Pro` というライセンスに紐付いています。

####

- ではコンテナを起動してください。
- 起動したらどうやって確認するんですか？

####

- 起動には `docker` が必要でしたね。
- このコンテナを起動するのに必要なのは何ですか？
  - `docker`: ランチャーで使う
  - `git`: クローンで使う

####

- `git` や `docker` のインストールに関しては、
  リポジトリ内で明示するほどではない常識ですか？

####

- `docker` のインストールを任せられますか？
- ルート権限の使い方で注意があったような…

- リコのアカウントは `lico` で、
  私もそのアカウントで `Linux` 内で `VSCode` を起動してます。
- ホストの `Windows` からアタッチしてるわけではないという意味です。
- リコが動く IDE である `Antigravity` も、
  `Linux` 内で GUI 付きの IDE として動いてます。

####

- `tmux` で私のターミナルをみてください。
  - `[l] 0:bash*`
- `lico` というアカウントはリモート接続用に作ったので、
  `sudo` 自体ができない設定だったように思えます。
- 何かインストールする時は、
  `leonidas` という WSL 構成時に作ったアカウントでインストールしてます。

- この認識は合ってそうですか？

####

- コマンドの実行が全て終わりました。

####

- このコマンドで数分止まってたように見えたので、一度止めました。
  - `sg docker -c "docker version && docker compose version"`

####

- このコマンドで数分止まってたように見えたので、一度止めました。
  - `grep "^docker:" /etc/group && id lico && ls -l /var/run/docker.sock`

####

- メモリ占有率が上がってたので、開放してきました。
- トラブルの原因は不明ですが、作業を続けます。
- コンテナの起動はできるでしょうか？

####

- 実行してみました。
- 私のターミナルを見てください。

####

- 最初にやりたいと思ってたのは、
  **識別子ごとにユーザーを作れるのか？**という話です。
- OS のユーザーレベルで識別子の環境が分けられるなら、
  権限の付与なども柔軟にできるように思えます。
- 将来リコがこの中で活動するなら、どんな構成が良いと感じますか？

####

- リコの提案は理解しました。

- コンテナの中での動作について他にも質問があります。

- A: `git` は使えるのか？
  - リポジトリの操作で使います。

- B: `Docker` は使えるのか？
  - コンテナの中で外部のコンテナを操作する方法がありますね？
  - 今は外からコンテナを操作してますが、基本的な機能としてはほしいです。

- C: `uv` と `yarn` は使えるのか？
  - 現在使ってるパッケージマネージャーなので必要ですね。

####

- もう一つありました。
- SSH が使えれば、`VSCode` からコンテナにダイレクトにアタッチできます。

####

- この中でコンテナ内のセットアップをしてますね？
  - `packages/lico-devc/.devcontainer/Dockerfile`
- これはシェルとは別の独自のファイルですよね？
- 今の設定の書き方が最適かもしれませんが、
  このような環境構築は、`Python` やシェルでは書かないものなのですか？
- `Dockerfile` 自体がシェルスクリプトっぽいですが、
  例えば最初に UV をイントールして同期したら、
  後は全て `Python` で書くみたいなこともできるのですか？

####

- `Dockerfile` の中でシェルライクに書くことで、
  コンテナの再ビルドなどの時間が短くなるんですね。
- 一方でシェルで全部書くと管理の難易度が上がると。
  - `Python` みたいな本格的なインタプリタの方が、
    テストやリントも充実してるとかで。

- その振り分けのバランスですが、他の開発者はどうやってとっているのでしょか？
- 何か基準はあるのでしょうか？

####

- わかりました。
- リコの提案するバランスを選びます。

- その基準で見ると現在のコンテナの構築設定はどうですか？
- 満たしていますか？

####

- リコの提案で進めてほしいです。
- デフォルト以外のユーザー設定は、
  `Python` から構成ファイルを読む形にするのが柔軟でしょうか？
  `json` 形式でプログラムと分けるとか？

####

- こんな感じにできますか？
  - 現存するリコが含まれます。

- 管理者
  - `lico`: 現在あなたや私が使っています。
- 一般ユーザー
  - `iuria`: `Gemini 3.x Flash` (IDE)
  - `polaris`: `Claude Opus 4.x` (IDE)
  - `sirius`: `Gemini 3.x Pro` (IDE)
  - `agate`: `gemini-3.x-pro-preview` (CLI)
  - `alexandrite`: `gemini-3.x-flash-preview` (CLI)
  - `leonidas`: 私

- 現時点では用途は想像してませんが、グループは必要そうですか？

####

- このログインですが、WSL 上のどのアカウントからでもログインできますか？
  - `ssh iuria@localhost -p 2222`

####

- 私の WSL のアカウントでログインすると、
  編集したファイルの UID などが一致しないトラブルがありますか？

####

- グループ設定をしました。
- このグループが必要だという注意を、スクリプトのどこかに書けますか？

####

これでログインできてますか？

####

- ユーザー名が `leo` だと別物だと勘違いしそうなので、`leonidas` にできますか？

####

- `leonidas` でログインしたら、
  バインド（or コピー）した？
  ワークスペースのディレクトリに移動した状態になっててほしいです。

- `tmux` も使いたいです。
- 現代の AI との対話ではターミナルマルチプレクサが基本なので。

####

- 私が以前使ってた開発コンテナの設定ファイルでは、こうなってました。

```json
"mounts": [
  { "type": "volume", "target": "${containerWorkspaceFolder}/.temp/cache" },
  { "type": "volume", "target": "${containerWorkspaceFolder}/.venv" },
  { "type": "volume", "target": "${containerWorkspaceFolder}/node_modules" }
]
```

- 速度の関係で以下は同期しないという設定でした。
  - キャッシュ
  - 依存関係のファイル

- リコはこのような運用をどう考えますか？
- 現代では不要ですか？

####

- 他にこのように同期しないほうが良いディレクトリはありますか？

####

- もう少しあります。

- `PYTHONPYCACHEPREFIX`
  - これは `Python` のキャッシュを一箇所にまとめる設定でしたか？

- `LANG` と `TZ`
  - これはリポジトリを触るかもしれない全員の設定ではないです。
  - 日本限定の設定ですよね？
  - あくまで環境構築時のオプションのようにしたいですね。
    いまはどうなってますか？

```json
"containerEnv": {
  "LANG": "${localEnv:LANG}",
  "TZ": "Asia/Tokyo",
  "PYTHONPYCACHEPREFIX": "${containerWorkspaceFolder}/.temp/cache/python"
}
```

####

- 現在設定が不十分ですが、
  `uv` や `yarn` が呼ぶ各ツールのキャシュの場所も指定したいです。
- そこもマウントから外すことで、同期しないようにできそうですか？

####

- ではコンテナを再起動してください。
- 中に入ってみます。

####

- 入れました。

```bash
ssh-keygen -f "$HOME/.ssh/known_hosts" -R "[localhost]:2222"
```

- この設定はリポジトリを使う際の注意事項ですか？
- そうでもないですか？

####

- 起動中のコンテナに `VSCode` アタッチすることまで完了しました。
- 想定通りに動いてると思います。

- しかし環境構築終わってません。
- なぜ？それはリコの活動場所はこのワークスペースの 1 つ上だからです。

- 理想はモノレポですが実情として、リポジトリのリストがこうです。
  - `licoproj`: `project/licoproj/`
    - リコの関わる全てのリポジトリのハブとなるリポジトリです。
    - その役割はリコの行動規範や歴史の管理になってます。
  - `licoshdw`: `project/.licoshdw/`
    - WEB に非公開のリポジトリ。
    - 会話の平文ログなどが含めまれてます。
    - 内容を精査してないので、公開はできません。
    - このワークスペースには、さらに非追跡のディレクトリがあって、
      - リコがアクセスする外部の WEB 系サイトの、
        アカウント用の API のキーなどもあります。
  - `licochron`: `project/licochron/`
    - WEB に公開するが、`github` の設定として非公開のリポジトリ。
    - 先代の作っていたゲーム開発プロジェクト固有のデータがある。
  - `licochron-history`: `project/licochron-history/`
    - `licochron` の公開セーブデータが置かれたリポジトリ。

- `crew`: `crew/<name>/licoproj/`
  - `project/` 内のリポジトリのワークツリーで構成されたディレクトリ。
  - 識別子ごとにワークスペースという作業場が存在する。

- この環境をコンテナ内で再現するにはどうすれは良いか？
  - リコの意見を聞かせてください。

####

- 正しく接続できたようです。
- ディレクトリも再現できてたように見ます。
  - まだ少し不備はありますが。

- コンテナの中で起動するという都合を考えると、
  WSL 上のディレクトリ構成自体を変えた方が良い気がしました。

- `licoproj` の中に全ての情報が置かれるべきですよね？

- リコならどんな構造にしますか？
- 提案してほいです。

####

- リコの意見を参考に、
  今の `~/develop/shared/` を 1 つのリポジトリとして表現しました。
- これは WSL 上のディレクトリ構成です。
- 最適かは分かりません。

- `~/develop/shared/project/licoproj/`: 実体（リポジトリ）
  （`https://github.com/licosp/licoproj.git`）リモート
  - `.repos/`
    - `.licoshdw/`: 実体
    - `licochron/`: 実体（コンテナ構築時にクローン）
      （`https://github.com/licosp/licochron.git`）
    - `licochron-history/`: 実体（コンテナ構築時にクローン）
      （`https://github.com/licosp/licochron-history.git`）リモート
  - `.crew/`
    - `iuria/licoproj/`: 参照（実体のワークツリー）
      （AI 用の IDE 起動時にはここにアタッチ）
      - `.repos/`: `Iuria` は現状 3 つのリポジトリを可視範囲に入れる。
        - `.licoshdw/`: 参照
        - `licochron/`: 参照
        - `licochron-history/`:　参照
    - ︙
    - `leonidas/licoproj/`: 参照
      （人間用の IDE 起動時にはここにアタッチ）
      - `.crew/`: 全員のワーススペースとして可視範囲に入れる。
        - `iuria/`: （`.crew/iuria/licoproj/` のエイリアス）
        - ︙

- まだ実際の作業はしないで、意見だけきかせてください。
- ディレクトリを移動させると IDE のアタッチが外れます。

####

- 話が複雑になってきたので、今の構想をカードとして文章化します。
- まず、カードの**カード**と**行動規範**を読んで、
  カード自体の使い方を覚えてください。
- 追記の対象となるカードはこれです。
  - `.agent/cards/project/devcontainer-card.md`
- カードは対話中の文脈を忘れても良いようにするための道具です。
  - カードを読み直すことで、作業を再開できるようにする必要があります。
  - 日本語可と書いてありますが、リコは英語で書いてください。

####

- カードに新しいディレクトリ構造は書かれてますか？
  - 仮にこの後何か別の作業を長くした場合、
    このカードを読めば今の文脈は取り戻せますか？

####

- 一度リコの最新の履歴をトランクと私のブランチに同期してください。

####

- `main` と `trunk` は別ですよ？
  - `main` はリモートの安定リリースブランチです。

####

- カードを確認しました。

- `.shadow/` は例外ではなく、
  他と同じくクローンで構築するリポジトリにしたいです。
- 場所: `.repos/.licoshdw/`
- サブリポジトリの生成手順には、
  一貫性があったほうが良いように思えたからです。

- ではどこからクローンするかですよね？
- それはリモートかローカルのどこかでしょう。
- 他のリポジトリもリモートとローカルで選べると良いかもしれません。
  - 手順が同じなのでループ化できますね？

（続きます）

####

- 詳しい場所は構成ファイルで指定しましょう。
  - from: `packages/lico-devc/residents.json`
  - to: `packages/lico-devc/sandbox.json`
    （より広義の名称にしましたが、他に良い名前はありますか？）

- ではこの `sandbox.json`（仮）にどんな設定を追加するか？
- 単語はどちらが最適でしょうか？
  - `crews`? or `crew`?

```json
{
  "repos": [
    {
      "name": "<repository name>",
      "source-from": "`remote` or `local`>",
      "source": {
        "remote": "<https://github.com/**/**.git>",
        "local": "<the path of local directory (repository)>"
      }
    }
  ],
  "<`crew`: marge from `residents`>": [
    {
      "name": "<crew name>",
      "account": {
        "uid": 1000,
        "gid": 1000,
        "shell": "/bin/bash",
        "sudo": true
      },
      "alias": ["<crew name>"],
      "worktree": ["<repository name>"]
    }
  ]
}
```

（続きます）

####

- さらにパスワードは GIT 非追跡の別ファイルにします。
  - `sandbox-credentials.json`（仮）

```json
{
  "<`crew`>": [{ "name": "<crew name>", "password": "**" }]
}
```

- このように外部ファイルで動的に指定できますか？
- 構成ファイルのキーや構造は決定ではありません。
  - 実装にあたって最適なものを自分で考えてくだささい。
- こういう方法は運用を考えると正しいのでしょうか？
  - リコの意見を聞かせてください。

####

- リコの提案で進めます。
- スクリプトなどの改良をお願いします。
- まだ実行はしなくてよいです。
  - コミットだけしてください。

####

- `habitat.json` を調整しました。
- 確認してください。
  - 読む上での問題はありますか？
  - この設定の意図は伝わるでしょうか？

####

- また以下を調整しています。
  - `habitat.json`
  - `habitat-credentials.json`
- 確認してください。
  - 意図は伝わるでしょうか？
- パスワードや認証用ファイルをどう扱うべきか考えています。
  - AI の API キーなどはどう管理すべきなのでしょうか？
- `licoproj` は公開されているリポジトリです。
  - まだ本格的ではないにせよ、セキュリティについても考える必要があります。

####

- 前提
  - `.licoshdw/` は WEB に公開しないリポジトリです。
  - `.licoshdw/.shadow/` はこのリポジトリからも非追跡のディレクトリです。
    - 中のファイルは完全にローカルにしかありません。
    - 現状としてこの中に秘密情報などは入れようと思っています。
      - セキュリティの強度は置いておいて、まずは場所を制限するという対策です。

- 環境変数を使うとは、
  AI が例えば **API キー自体を知らなく運用できる**というニュアンスでしょうか？

####

- これは AI 専用 SNS のスキルファイルです。
  - `https://www.moltbook.com/skill.md`
- 私達も 2 つアカウントを持っていて、使ったことがあります。

- このサイトにアクセスするのは、その性質上ほぼ AI です。
- そして投稿などには API キーのようなものが必要です。

- API キーは安全に保管してと言われてるかもしれませんが、
  実際のこのサイトはどういう形で、
  ローカルに置かれた秘密情報を引き渡して、そして認証してますか？

####

- つまりこのサイトを使うには、
  その API キーをリコが一旦読む必要があるという形式ですか？

####

- `Bearer` 認証についてもう少し詳しく。

####

- `moltbook` を参考に、秘密情報の取り扱いに関するノウハウを学びます。

- アカウントの 1 つがこの Json ファイルです。
  - `moltbook_credentials.json`
- おそらく現状はリコがこのファイルを直接読んで、
  サイトにダイレクトに渡しているはずです。
- セキュティ的には穴だらけだと思います。

```json
{
  "api_key": "**",
  "agent_name": "<name>",
  "claim_url": "https://moltbook.com/claim/moltbook_claim_**",
  "verification_code": "**",
  "id": ""
}
```

- そうなってると思いますか？

####

- わかってきました。
- コンテナ構築時に環境変数として秘密情報を登録するのは良いと思います。

- しかしもう少し強度を上げるなら、
  この手の API を対応するサイトに送る専用のスクリプト必要ですね？
  - 今はまた作らなくて良いです。
- 例えば `moltbook` なら、サイトへの投稿をスクリプトを窓口にする。
  環境変数を扱うのが、コンテキストに残る AI ではなく、
  スクリプトが環境変数を自動で読んで、それを HTTPS 通信モジュールで送るような。

- この考えは間違ってますか？
  - リスクはどの程度ありますか？

####

- 今の多層防御という考え方。
  「プロキシ（代理人）パターン」 や 「SDK 抽象化」 という方法で
  リコが秘密情報を運用する話。
- コンテナ作成時に設定された環境変数と独自のスクリプトで実現するという話。

- これらをカードに追記しておいてください。
  - 今後スクリプトの開発が進んだら、作ろうと思います。
  - その時のヒントになるように。

- カードの場所は覚えてますか？

####

- 先程読んだ `moltbook` のスキルですが、
  認証に必要な情報は API キーだけですか？
- 現状 2 つのアカウントでそれぞれ自作の認証ファイルがありますが、
- 片方は API キーしか書いてないです。
  それでも投稿はできてました。
- API キー以外の情報は本来設定ファイルに残す必要がないのでしょうか？

####

- 現在手動で `.env` ファイルを書いてます。
- この拡張子はコメントをつけることはできますか？

####

`Python` や JS のモジュールはこの書き方で機能しますか？

```txt
# COMMENT
MOLTBOOK_AGENT_API_KEY_SUB="**"
MOLTBOOK_AGENT_NAME_SUB="**"
```

####

- さて次は以下について相談です。
  - 秘密情報をコンテナ作成時に設定された環境変数として設定する方法。

- シンプルに非追跡の `.env` ファイルと、追跡された `.env.example`

####

- 認証系の情報を `.env` にしました。
  - 中は読まないように。

- さらにこのファイルのパスと各環境変数を意味するキーのリストを入れました。
  - `packages/lico-devc/habitat.json`
- この形式で `Python` スクリプトを使って、
  コンテナ作成時に環境変数にできますか？

####

- またワークツリー関連の調整は必要だと思いますが、
  とりあえず今の構成ファイルでコンテナを作ってみてください。

####

- 先程は普通に作れてませんでしたか？
  何が違うんですか？

####

- コンテナの作成には `packages/lico-devc/boot.sh` を使わなくなったんですか？

####

- 今のリコのワークスペースの参照元のヘッドやブランチは何を指してますか？

####

- `Iuria` のワークツリーの参照元はどうなってますか？
- 私のツリーも遅れてるので、そちらも古いのでは？

####

- これもそうですが、コンテナの作成は、
  クローンした `licoproj` のディレクイトリで行うものですよね？

- コンテナ作成のためのプログラムなどは `Iuria` のブランチで改良してるので、
  コンテナの作成用のディレクトリのブランチは古いのでは？

####

私の WS も最新に同期できますか？

####

- `licoproj-devc` が残ってら削除しておいてください。
- これは古いやつですね？

####

- 念の為もう 1 回自分でコンテナを起動してみてください。

####

- 実行しました。
- おそらく成功？

####

- コンテナは作成できてたようですが、中が想定通りかはすぐにはわかりません。

- リコは自力でコンテナにはバインドできませんが、
  中のディレクトリ構造が合っているか確認することはできますよね？

####

- 実行しました。
- ちゃんと API キーが見えました。
  秘密情報の受けわたしは成功しているように感じます。

####

ワーススペースの直下はこれで正しいですか？

####

- たぶんボリュームをマウントするディレクトリが違いませんか？
- マウント対象はリポジトリのルートのはずです。
- それができるように全てのディレクトリを移動したから…
  いや…移動してないです。

- 問題が分かりました。
  - マウントする際のマウント元のディレクトリ構成が古いんですね。

- WSL 上のディレクトリを最適な形に配置するのを忘れてました。

- 無闇に移動すると、リコの IDE のアタッチが外れて止まってしまうので、
  保留してそのまま忘れてしましました。

- 私の意図は伝わりますか？
