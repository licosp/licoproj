---
ai_visible: true
title: ""
description: ""
tag: [draft, scratchpad]
version: 1.0.0
created: 2026-02-27T00:00:00+09:00
updated: 2026-02-27T00:00:00+09:00
language: (en/ja)
author: leonidas
---

# Draft

## Questions and instructions

---

### `Antigravity` | `Gemini 3.1 Pro (High)`: `Planning` | `Sirius`

####

- 作業の途中ですが、日付が変わりました。
- テンプレートから今日の会話ファイルを作成して、追記用ファイルを移動してください。

####

お願いします。

####

- 次です。
- カードのフロントマターを同じパターンに微調整しました。
- まとめてコミットしてください。

####

- これを忘れていました。
  `2026-01-24T2315_resource-volatility-geopolitics.md`

####

次はこれです。

- 下書きファイル
- リンター用構成ファイル

####

次は影に 2 つの会話をコミットしてください。

####

- 次は**コマンドシム**の作業をするで、関連するカードを読んでください。
- 実はこの機能は現在 OFF になっています。
- OFF の状態でコマンドを使う機会があったからです。
  その時これは（ON/OFF）を切り替えられないと困るな？と感じました。
- デフォルトはオフの状態として、これを切り替える何か良い方法はありますか？

####

- 実体のある `Option A` が分かりやくて良いと感じます。
- 例えばそのファイルを YAML 形式の構成ファイルにして、
  **機能フラグ**のように使う計画はどう思います？
- コマンドごとに ON/OFF できるような？

####

- このコマンドシムというのは便利なので、数も機能も増えて複雑化する傾向が出てきました。
- 例えばこの後以下の計画も考えています。
  - Python の標準コマンドを uv の提供する python ランタイムに切り替える
  - コマンドシムの仕組み自体を Python で書く
    - シェルコマンドでやるには荷が重いので
- リコはこのような発展がある点をどう考慮しますか？

####

- 実装の質問です。
- 例えは `rm` コマンドが呼ばれた場合、
  `rm` のダミーとなる最小の処理しか持たないコマンドを用意しておいて、
  それが実行されるとルーターのような Python スクリプトを呼ぶのでしょうか？

- 会話ログへの追記も並行して行ってください。

####

- 納得しました。

- 次の実装の質問です。
- コマンドシムで Python を切り替える機能を実装する場合、
  最初に呼ばれる Python だけデフォルトの Python が動くというニュアンスですか？
- Python で全体を書くけど、
  その大部分は uv の提供する python で実行されるような形にできそうですか？

####

- では実際に作ってみてください。
  できたら確認します。

####

- この 3 つのコマンドでスクリプトのチェックと修正を試せますか？
  > uv run ruff check --no-fix .agent/scripts/lico-shim.py
  > uv run ruff format --check .agent/scripts/lico-shim.py
  > uv run mypy .agent/scripts/lico-shim.py

####

- リントの条件を少し厳しくしました。
  直せそうですか？
- `uv run ruff check --no-fix .agent/scripts/lico-shim.py`

####

- リントの条件をさらに少し厳しくしました。
- 誘導したくないので意図は言ません。
  リコがどう直すか見たいです。

####

- このリンターの警告でコードを抽象化するだろうか？と考えていました。
- 現在こんなコードが出てますが、それが整理されるのではないかと期待していました。
  - 関数にまとめる
  - リテラルの定義と使用を分ける
- メッセージが直接的ではないというのは分かりますが、
  **より多くの抽象化を行ってほしい**という状況に対して、
  Ruff で告を出させる方法がわかりませんでした。

```python
logger.error(" ... ")
logger.error(" ... ")
logger.error(" ... ")
```

####

- リンターの警告だけでやるのは限界があるということですね。

- 例えば pyproject.toml のこの項目を見てください。
  これらにコードの保守性を高める効果はあるでしょうか？

  > [tool.ruff.lint.flake8-tidy-imports.banned-api]

####

- `banned-api` を使った使用禁止構文は現時点では構成ファイルから除外しておきました。
  効果はありそうですが、場当たり的になっているので、また次の機会に考えます。

- もう一点質問があります。
- 私の IDE の `VSCode` では、`Pylance` から以下のエラーが見えています。
  `.agent/.internal/workspace/pylance-error.json`

- これは何故 `Ruff` や `mypy` では検知しないのですか？

####

全ての変数に型ヒントを書いてほしいですが、これはリンターの警告では出せないのですか？
