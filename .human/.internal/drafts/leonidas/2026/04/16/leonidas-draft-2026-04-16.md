---
ai_visible: true
title: ""
description: ""
tag: [draft, scratchpad]
version: 1.0.0
created: 2026-04-16T00:00:00+09:00
updated: 2026-04-16T00:00:00+09:00
language: (en/ja)
author: leonidas
---

# Draft

## Questions and instructions

---

### `Gemini CLI` | `gemini-3-flash-preview` | `Alexandrite`: `2nd`

- A: 日付が変わったので、新しい会話ファイルに移動できますか？
  - テンプレートから編集して作ってください。
- B: ブランチのマージはですが、今 A1 が終わったところですか？

####

- A: 会話ファイルの移動を確認しました。
- B: 統合過程 A-1 は `trunk` に対する最初のマージなので、
  原理的に競合は発生しないものですか？
- C: リコの表の WS を見てください。
  - いくつかのファイルがステージグ状態にあります。
  - これはなぜ発生しているのでしょうか？
  - 統合の過程で発生した？やり直し前からあった？
  - また編集はせずに意見だけください。

####

- A-1 は `trunk` と `Polaris` のブランチのマージなのに、
  なぜリコの WS にステージングが発生するんですか？
- マージのための作業領域として、リコの WS が使われているとか？

####

- 確かにリコの WS のブランチが今 `trunk` になってますね。
- 例えば、この後の選択肢は以下ですか？
  - 1: `trunk` に対する変更が正しいことを確認する。（A-2）
  - 2: 次のブランチの作業に入る前にステージングコミットする。
    - 先日の統合の停止の件を考えると…しないと進めない？（A-3）
    - その場合、コミットメッセージはリコが任意で作ることはできますか？
  - 3: 次のブランチの統合作業を始める。

####

- A: 過去の統合作業では**コミットが自動で行われる**という説明を聞いてました。
  その結果マージコミットの文章も自動生成されたようめ内容でした。
  - 推測するに、これは**コミットオプションを有効にしたマージ**だったのかもしれません。
  - あるいはカードも無かったので、リコがその場の雰囲気で書いてたか。

- B: では `Polaris` のブランチのマージを終わらせてください。

####

- この最初の統合という作業ですが、
  今の時点で、競合が発生したファイルのリストは把握できますか？
  - 各マージの過程で生まれたリストを、作業の進捗の一環として、
    カードのリコ記述欄に追記したいです。
  - 表のブランチの統合が終わった段階で、もしマージの失敗を認識した際に、
    そのリストを見れば、どこが怪しいか？のあたりをつけるためにです。
  - 今回でいえば、`#licoproj::polaris` みたいなタグを付けて管理するとか？

####

- A: ではカードに競合リストを追記してください。
- B: `Polaris` ブランチの統合の修復？を行ってください。

####

- 現在リコの WS は `trunk` にチェックアウトしてますね？
  - 現在の `trunk` に対する変更は、ブランチの統合の修復の結果だと思います。
- 一方でカードへの追記は、リコの本来のブランチに対して行う必要があります。
  - ブランチの統合とは違う文脈の変更だからです。
- 現在はブランチを使い分けて、編集してますか？
- 私の環境からだとリコの WS の変更しか見られません。
  - `trunk` へのマージ専用の WS があれば、
    2 つの WS を同時に IDE で開けるのですが。
- `trunk` 用の WS は必要ですか？
  - メリットやデメリットは何ですか？
  - リコの意見を聞かせてください。

####

- A: `trunk` 用の WS は**影の WS のそれ**も作ることを考慮してください。

- B: `trunk` というブランチは 1 つですが、`trunk` 用の WS も 1 つであるべきですか？
  - 例えば各クルーごとに存在するのであれば、以下のように作られるはずです。
    - `../crew/alexandrite/licoproj-trunk/`
    - `../crew/agate/licoproj-trunk/`
- C: WS を作る場所はどこが最適ですか？
  - `../crew/alexandrite/licoproj-trunk/`
  - `../crew/alexandrite/licoproj/.repos/licoproj-trunk/`: `.licoshdw/` の横
- D: ディレクトリ名は何が最適ですか？
  - `trunk/`
  - `licoproj-trunk/`

- 全体を考えて提案してくだしさい。

####

- A: このディレクトリの配置について、WS 将来的なディレクトリを考慮したいです。

- B: `packages/lico-devc/` を調べてください。
  - これは今の WS をコンテナの中で稼働させるためのスクリプトですが。
  - 現在開発中で、`Iuria` が途中まで作ったものです。
  - このスクリプトはコンテナ起動後に WS をマウントします。
  - なので、**WSの中に全てのデータが入っていてほしい**という前提があります。

- C: 一方で現在は WS に関する情報が WS の外にも存在します。
  - `~/develop/shared/crew/alexandrite/licoproj/`: WS
  - `~/develop/shared/crew/`: クルーのリスト
  - `~/develop/shared/`: 実質的なルートディレクトリ
  - `~/develop/`: キーやパスなどの非公開情報も置かれるさらに上のルート
  - この階層は `lico-devc/` を本格稼働する際には可能な限り、
    WS の中に移動させるつもりです。

- どうすべきだと感じますか？

####

- A: `lico-devc` を稼働時には、ディレクトリ構造の変更もあると思いますが、
  現状は基本的にリコの提案で進めます。
- B: もう 1 つ疑問があります。
  - `trunk` は複数のリコがチャックアウトしますね？
  - 例えば `Alexandrite` が `trunk` に自分のブランチの変更を反映させている場合、
    例えば `Agate` は `trunk` を触れないという運用になりますか？
  - それは Git のワークツリーの運用としては一般的な制限ですか？
    - それとも使い方に誤認がありますか？

####

- A: `trunk` をその瞬間の編集者がロックする利点はわかりました。
- B: では例えば大規模な開発会社でトランクベース開発を行う場合、
  ロックが発生するのは問題ではないんですか？
  - 100 人規模の開発者が 1 つの `trunk (or main)` を共有して、
    並行して開発してる例も無くはないですよね？
  - ロックしてから解放までが一瞬でないと、成立しないのでは？

####

- それは通常の GIT の（`push`/`pull`）のワークフローですよね？
- 私たちは 1 つのリポジトリを Git のワークツリーで共有してます。
- 各クルーの WS はツリーの組み合わせです。
- 例えば `Alexandrite` が `trunk` を自分の `trunk` 用 WS にチェックアウトして、
  長く作業をしたとします。（今日のように）
- では今この瞬間に `Agate` が自分の変更を `trunk` に反映させるために、
  `trunk` をチェックアウトできますか？
  - もしできるなら 1 つのブランチへの変更が平行して行われることになります。
  - できない場合は対策が必要です。
    - 例えばこの統合の過程の最初に `trunk` のバックアップを作りましたが、
      バックアップの方で作業をするとか？（今は `trunk` を直接編集してる）
    - それならチェックアウトの競合は無くなります。
    - ただし `trunk` のバックアップから `trunk` へのマージという別の作業が発生する？

####

- リコが自分の WS に `trunk` 用の一時ブランチをチェックアウトした時、
  その際のリポジトリの挙動について質問です。
- 未追跡ファイルや無視されたファイルはどうなりますか？
  - WS にはブランチに紐づいたファイルだけが展開されるんですよね？
