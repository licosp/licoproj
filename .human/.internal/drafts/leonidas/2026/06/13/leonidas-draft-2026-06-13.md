---
ai_visible: true
title: ""
description: ""
tag: [draft, scratchpad]
version: 1.0.0
created: 2026-06-13T00:00:00+09:00
updated: 2026-06-13T00:00:00+09:00
language: (en/ja)
author: leonidas
---

# Draft

## Questions and instructions

---

### `Anthropic Claude` | `Sonnet 4.6 (Low)` | `Second-Eye`: `2025-06-09`

####

- では `llama.cpp` の話しに戻ります。
  - `uv` を使いリポジトリに `huggingface_hub` をダウンロードしました。
  - さらにスクリプト実行環境として、`lico-models` というサブプロジェクトを作りました。
  - ライブラリをインポートして、実行するとことまで作れました。

####

- A: `manager.py` はこれだけです。

- B: モデルのダウンロード先は `~/develop/.llama/models/` で良いですか？

- C: まずは `ollama` で使っていた、
  `gemma4:12b-it-qat` に相当するものをダウンロードしたいです。
  - スクリプトからモデルの検索とかもできるんですか？

####

- 検索しました。

####

- 取得しました。

####

- MTP を使うケースについて。
  - 明らかに VRAM が足りないという構成ですか？

####

- ダウンロードが終わりました。
- まず添付ファイルに、
  ビルドした `llama-server` の `help` を載せています。

####

- スクリプト化して実行しました。
  - エラーが 1 つ出ている？

####

- なるほど。
- 今後は 2 つのスクリプトの改良をします。
- 気になる点。
  - `thinking` フラグは有効になっているか？
  - `start-llama.sh` のパラメータの一部を、
    `talk-llama.sh` に移動させれるか？
    - このサーバーを起動しても、
      モデルの切り替えがコーディングエージェント側からできない気がします。

####

- 起動するモデルことにサーバーが必要という点は把握しました。
- 共用の変数はありますが、モデルごとに変えたい変数は、
  何が別のファイルにして、引数でそれを指定するような形式は可能ですか？

####

- 調べました
  - `models.ini` というファイルを使うことで、
    単一サーバーでもモデル切り替えができるそうです。
    - `.llama/assets/models.ini`
  - ただしモデルの切り替えで、アンロード/ロードが入るかも？と聞きました。
  - 全てのパラメータが機能してるかは確認が必要です。
- またモデルのダウンロードパスを調整しました。
  - さらにマルチモーダル用のファイルもダウンロードしました。

####

- `ini` を修正しました。
- 以前書いてもらった ollama 用のテスト会話ファイル、
  今こうなってます。
  - `.ollama/tools/ollama-talk.sh`
  - これと同じ機能を `talk-llama.sh` に持たせたいです。

####

- `llama-request.json` ですが、思考ブロックの有効化フラグは無いのですか？
- それとも常時 ON？

####

- 対話テストをしました。
  - 本格的なクエリだったの、`tps` は下がりましたが、
    それでも `ollama` 版が 30 前半だったことを考えると、高速化されてると感じます。

####

- デバッグログの `"created": 1781359935,` について、
  タイムスタンプの書式を変えたいです。
  - 以下のフォーマットにできますか？
    - `2026-06-13T17:45:00+09:00`
  - タイムゾーンはアジア/東京でしたか？

####

- A: 対話実験をした結果、タイムスタンプは直ってました。
  - `"created_at": "2026-06-13T23:24:55+09:00"`
- B: `ollama` の方も修正してほしいです。
  - 今はこんな書式です。
    - `"created_at": "2026-06-12T19:22:25.535240082Z",`
  - `jq` ファイルは以下です。

####

- `ollama` の方も直りました。
- 機能するかは分かりませんが、
  現在モデルファイルのパスの調整をしてます。

- 現在 CWD はここだと過程します。

```bash
cd ~/develop/shared/crew/protostar/licoproj/
```

- その状態で、A のシンボリックリンクを、B のように作るコマンドを教えてください。
  - A: `~/develop/.llama/models/`
  - B: `.temp/llama/models/`
