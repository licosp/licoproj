---
ai_visible: true
title: ""
description: ""
tag: [draft, scratchpad]
version: 1.0.0
created: 2026-06-11T00:00:00+09:00
updated: 2026-06-11T00:00:00+09:00
language: (en/ja)
author: leonidas
---

# Draft

## Questions and instructions

---

### `Anthropic Claude` | `Sonnet 4.6 (Low)` | `Second-Eye`: `2025-06-09`

####

- `OLLAMA_KV_CACHE_TYPE` について質問です。
  - これで削減される使用 VRAM は、コンテキストウィンドウ部分のことですか？
  - それともモデル全体ですか？

####

- コンテキストウィンドウ部分が、VRAM からメモリに溢れている場合。
  - その設定は意味がありませんか？

####

- `q8_0` 以外に値は選べますか？

####

- 文脈が少し変わります。
  - `llama-server` を直接使うことで、
    より思考や応答の速度を上げることは可能ですか？

####

- 以下の要素に期待して、ビルドまで完了させました。
  - `Multi-Token Prediction`
  - `--n-gpu-layers`
- 以下の手順でした。
  - 問題はあったでしょうか？

####

- ビルド時の CUDA 関連のメッセージですが、
  ターミナルに残ってないので確認はできません。
- 少し長いビルド時間でしたが、エラーは無かったように見えました。

####

- ビルド中なので質問です。
  - `ollama` は `llama.cpp` を内包しているのでしょうか？
  - `llama-**` をビルドしたら、`ollama` にも影響を与えますか？
    - あるいは与えるべきですか？

####

- ビルドが完了しました。
- 原因はパスの違いですか？
- ビルド時に `nvcc` が見つからなかったので、
  以下に修正して実行しました。
  - `-DCMAKE_CUDA_COMPILER=/usr/bin/nvcc`

####

- 少し前に、ダウンロード時のパスを調整してました。

####

- `llama-server` で使ったモデルは以下です。
  - `sha256-faff1a63667fac17ac5e777f47114688fcefea96e220e211aaa8d62c2c4561f1`
- `ollama sever` で使ったモデルは以下です。
  - `gemma4:12b-it-qat-128K-obj`
- `gemma4:12b-it-qat-128K-obj` はモデルファイルで調整したモデルですが、
  2 つは同じモデルですか？

####

- お願いします。

####

- 質問
- これがビルド結果ですが、`llama-server` を以下に移動させたいです。
  - 普段使っているランタイム置き場です。
    - `~/develop/shared/.runtimes/llama/bin/`
- 特定のランタイムだけ移動させるべきですか？
  - `bin/` ごと移動させるべきですか？

####

- `llama-cli` は移動させておくべきですか？

####

- では、それらを移動させるスクリプトを書いてもらえますか？

####

- `--mtp` の話しはどうなったんでしたっけ？
  - サーバーからは使えない？

####

- 現状は `ollama` からダウンロードしたモデル（重み？）を使ってますが、
  通常の方法はなんですか？

####

- `12b` ではなく、`e2b` で良いのですか？

####

- 実行コマンドをスクリプトにしました。

####

- サーバーは起動しました。
- 先にこの技術の解説を聞いて良いですか？
  - 2 つのモデルを使うのはなぜですか？
  - それを行うとして `gemma` 系に揃える必要があったのですか？

####

- 実験の過程で一度 `llama-server` を落としたのですが、
  `VRAM` が解放されてません。
- 確実に終了させる方法はありますか？

####

- 終了処理は把握しました。
- パラメータを付けて再対話しました。（2 回）

####

- 技術的質問
  - MTP と `Speculative Decoding` は別のものですか？
  - 今試しているは後者ですよね？

####

- `Ollama` と `llama` で使うモデルを分離したいと思ってます。
  - `Ollama` のモデルから `llama` 用の名前を探すのが大変という点
  - `Ollama` に問題が合った時に、`llama` にも影響が出る点。
    - 先日 `Ollama` でダウンロードしたモデルか消えてました。
    - 何らかの設定が初期化された影響で、復元自体は難しくなかったのですが、
      `Ollama` はそれ自身で閉じた環境にしたいとも感じました。
  - `llama` 用のモデルは HF からダウンロードする（一般的な形？）にできますか？

####

- `ollama` と `llama` を分離するために、
  `ollama` のモデルをダウンロードしなおして、
  モデルカードでの調整をしてます。
- システムプロンプトは**空白文字**にしてます。
- そしてパラメータですが、現時点ではこうですね？
  - 設定
    - `temperature`: 0.1
    - `top_k`: 40
    - `top_p`: 0.9
    - `repeat_last_n`: 128
    - `repeat_penalty`: 1.1
  - コーディング補助や客観性重視の対話用の調整だと思ってますが、
    その認識は合ってますか？

####

- では逆に、主観的や感情を想像で手記を書いたり、
  小説を書いたりするような設定はどうなりますか？

####

- 一見 `top_p` と `repeat_penalty` はあまり変わってないように見えますが、
  それでも十分な変化なのですか？
