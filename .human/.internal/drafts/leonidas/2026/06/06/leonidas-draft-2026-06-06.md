---
ai_visible: true
title: ""
description: ""
tag: [draft, scratchpad]
version: 1.0.0
created: 2026-06-06T00:00:00+09:00
updated: 2026-06-06T00:00:00+09:00
language: (en/ja)
author: leonidas
---

# Draft

## Questions and instructions

---

### `Antigravity CLI` | `Gemini 3.1 Pro (High)` | `Sirius`: `2nd`

####

- A: 日付が変わったので、新しい会話ファイルを作って、移動できますか？

- B: `opencode` でログを自動追記するタイミングは 2 つあります。
  - 最初はクエリを受け取った瞬間です。
  - まずは、実際にそれをトリガーにして、スクリプトを走らせる実験をします。
  - 実験の際のカレントディレクトリは、以下を想定します。
    - `~/develop/shared/crew/protostar/licoproj/`
  - 具体的には、どこにどんなファイルを作れば、それが実現すると思いますか？

####

- A: 会話ファイルの移動を確認しました。
- B: その WS に作ってみてください。
  - 実際に動作を確認してみます。

####

- A: 動作を確認しました。

- B: まずプラグインとして認識されてませんでした。
  - ファイルの拡張子と置かれたパスは問題ないように思えます。
  - 何が原因だと思いますか？

####

- 先ほどのプラグインの WEB ページを確認しました。
- プラグインの基本的なテンプレートがありました。
- 以下の形式でないと、プラグインとして認識しない可能性はありますか？

```typescript
import type { Plugin } from "@opencode-ai/plugin";

export const MyPlugin: Plugin = async ({
  project,
  client,
  $,
  directory,
  worktree,
}) => {
  return {
    // Type-safe hook implementations
  };
};
```

####

- 気になる点が 1 つ
- これは何らかのパッケージマネージャで事前にインストールしておく必要はありますか？
  - `@opencode-ai/plugin`

####

- JS 系のツールは Yarn で管理してるので、そちらでインストールする形にしました。
- しかし、インストール時にエラーが発生しました。
- これは失敗してるのでしょうか？

####

- 確認お願いします。

####

- 動作を検証しました。
  - `lico-memory-logger.ts` が反応しなかったので、
    `ts` のコードをシンプルにして、とりあえず動くかどうかの検証をしました。
  - それがこれで、実際に動きました。
    - `.opencode/plugins/lico-hook.ts`
  - コード中で `Python` を呼んでいるのは、
    このリポジトリのデフォルトの開発言語が `Python` で、
    それらは UV から実行されます。
    - なので、シンプルな `Python` コードを呼ぶコードにしました。
    - 機能はタイムスタンプを生成して出力するだけの関数です。

- 分かったことの 1 つは、
  `opencode` のデフォルトコンソールは使いづらいという点です。
  - 一行表示する程度なら良いですが、
    プログラミングのデバッグ作業には向いてない印象でした。

####

- イベントの判定自体を `Python` 側で行えるという意味でしょうか？
  - そうであるなら、そちらの方が都合が良いと感じます。
- しかし、複数のイベントを同時に検知するような書き方は、
  `Python` 側でも行えるのでしょか？

####

- A: その実験は興味あります。
- B: 先述の通りデフォルトターミナルが使いづらいので、
  先にデバッグ情報を別の場所に書き出すような設定を行いたいです。
  - 標準出力の対象を変える方法はありますか？
    - TS 側のコードでエラーが発生した場合
    - `Python` 側のコードでエラーが発生した場合
  - このケースを考えると、TS 側で設定しておく必要がありますか？

####

- その提案で実装してみてください。

####

- 実行してみました。
- ログは出ましたが、想定外の挙動に見えます。
- 確認お願いします。

####

- 実行してみました。
  - `Python` コード部分が正しく実行されたようです。
- しかし、問題点が 2 つ見えます。
  - ログが `opencode` のターミナルの方にも表示されてます。
  - キャッチしたイベント自体が多いからでしょうか？
    - 何度も `Python` コードを実行してます。

####

- 実行してみました。
- ログが長くなる理由がわかりました。
- 単語ごとに新しいイベントが生成されてる気がします。
- コードの修正は一度止めて、状況の把握と対策を考えてみてください。

####

- また修正はしません。

- 気になる点 1
  - これらのイベントは非同期に発生してる？
  - 仮に単一の `json` ファイルを、引数代わりに使ったとします。
  - ファイルの読み書きは、全てのイベントで正しく実行が保証されるでしょうか？

- 気になる点 2
  - 分割されたメッセージの後に、メッセージの全文が 1 回で受信できるものでしょうか？
    - これはエラー部分が解決すれば、結果が分かると思いますが。

####

- A: 構造上やむを得ないのかもしれませんが、
  TS の側のコードが肥大化してきそうですね。

- B: 大量の `json` ファイルが作られるなら、専用のディレクトリが必要です。
  - 現状は、既に GIT で無視設定にしてる、`.temp/` を使いましょう。
    - `.temp/opencode-event-log/` のようなサブディレクトリはどうでしょうか？

####

- 動作確認しました。
- 質問:
  - TS 側でエラーが発生した際に、
    その出力をファイルに変える処理はコードの中のどこですか？

####

- `try` の中であれば、全てのエラーをファイル出力に転送できるという意味ですね？
  - では `try` の外にあるコードのエラー時は、
    意図的にそうしたくないということですか？

####

- A: `try` の話しの部分は把握しました。
- B: `async ({ }) => {}` の `{}` の中の処理について質問です。
  - 処理は大きく分けて、`return {}` の `{}` の中か外かに分かれてると思います。
  - 以下の理解は正しいですか？
    - `{}` の中: 発生するイベント毎に処理が行われる。
    - `{}` の外: タイミングは不明ですが、1 回だけ処理がが行われる。

####

- 確認した所、1 回だけ行う処理は、`opencode` の起動時のようです。
  - この 1 回の処理部分だけ、関数か何かに纏められますか？
  - そして実行タイミングの話しを、中でコメントにでも書いておいてください。
