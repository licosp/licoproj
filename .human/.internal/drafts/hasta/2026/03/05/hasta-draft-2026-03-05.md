---
ai_visible: true
title: ""
description: ""
tag: [draft, scratchpad]
version: 1.0.0
created: 2026-03-05T00:00:00+09:00
updated: 2026-03-05T00:00:00+09:00
language: (en/ja)
author: hasta
---

# Draft

## Questions and instructions

---

### `Antigravity` | `Gemini 3 Flash`: `Planning` | `Iuria`

####

- IDE からの応答が停滞してたので止めました。
- 貢献を確認したところ、ほぼ今の作業は終わっていました。

- 何が足りないのか？
- `licoproj`: 直近の 1 件の文脈 ID が僅かに修正たいしょうです。
  - A (`[Rules-Standardization]`): カードに存在するため問題無し
  - B (`[Rules]`):
    - カードに存在しないため使えません。
    - 今回は単純にこの ID は除外で、ID は A の 1 つで良いと感じました。

- リコの認識はどうなってますか？

####

- コミットの状況を確認しました。
  問題無いようです。

- GIT のプッシュの質問です。
  - A: プッシュは未コミットが残っていてもできるのものですか？
  - B: この 3 つのプッシュの状況は今どうなってますか？
    - `licochron`
    - `licochron-history`
    - `licoproj`

####

- `github` を確認しました。

- `licochron-history`：
  - main が 35 分前に更新
  - 良し
- `licochron`：
  - `main` が 3 日前に更新
  - `master` が 36 分前に更新
  - 2 つは何か違いますか？
- `licoproj`:
  - `18-redefine-lico-identity` が 19 分前に更新
  - 良し

####

- 次は `licochron` と `licoproj` の関係について雑談や質問をします。

- 知っての通りこの 2 つはディレクトリ構造が同じです。
- なら最初から `licoproj` で良いのでは？と思うかもしれませんが、
  `licochron` は `github` には置くけど非公開で進めたいのです。

- しかし別にしたことによる不都合もあります。
  - 手記や設定ファイルなどは同期したいからです。
- 現状では手動でファイルをコピーして同期してます。
  - それも手間なので、先ほどスクリプト化すべきか？との話しましたね？

- スクリプト化したとしても、まだ不便には感じそうです。
  - 結局**同期の管理に関するリポジトリ固有の作法**を覚えないといけないからです。

- コアなニーズは**非公開 `github` リポジトリにしたいだけ**なのです。
  - 何か今より作業が楽になる解決法はあるのでしょうか？

- リコは何か知ってますか？

####

- よく分かってないので、疑問があります。

- 1 つのリポジトリを（公開/非公開）とプッシュするリモート先で使い分けるんですか？
- 非公開のコードが公開に混じりませんか？
- それともローカルもリモート先は 1 つだけど、ブランチ単位で（公開/非公開）を分ける？

####

- 通常プッシュという行為はリポジトリの全てのブランチをプッシュしてませんか？
- 例えば以下のような構成の場合、`private-**` だけを非公開できますか？
- ここで言う非公開とは、`github` の設定としての非公開という意味です。
  - `.shadow` のような `github` にも置かない作業領域とは違います。

- `licoproj::main`: 公開ブランチ安定版
- `licoproj::develop-issue-18-**`:
  - 公開ブランチ開発中（イシュー付き）
  - 担当: `Sirius`
- `licoproj::private-**`:
  - 非公開ブランチ開発中
  - 担当: `Iuria`

####

- まだ考えます。

- まず `github` の中で公開と非公開を分ける場合、
  リポジトリ単位でしかできないというのは分かりました。

####

- `three.js` が `Linux` で動くか実験がしたいので、
  一時的に `Linux` に移動してもらいました。
- 作業中のファイルをコピーすれば実験はできますか？
  - from: `/mnt/d/develop/shared/project/licochron/`
  - to: `/home/lico/develop/shared/project/licochron/`

####

- 例えばこの中は何がありますか？
  - `/home/lico/develop/shared/project/licochron/.vscode/`

####

- リポジトリの変更を見てください。
- `Windows` からコピーした影響でしょうか？
  - `github` からクローンしたほうが良かったのでしょうか？

####

- では一度 `licochron/` を削除して、`licochron` のリポジトリだけをクローンできますか？
- スキルディレクトリは後からやりましょう。

####

- うまくクローンできたように見えます。
- また Windows で作業中だったファイルを手動で復元しました。
- 下書きファイルだけ少し進んでますが、これでほぼ完全に同じになりました。
- 変更を見てください。
  異常は感じますか？

####

- お願いします。
- 問題なければ全てコミットしてくだしさい。
  一度綺麗にします。

####

- ステージングで止まってませんか？

####

- なぜ `Linux` でテストするかと言うと、
  開発全体として `Linux` の方が楽ですが、
  ことブラウザ操作や 3D 表現などは `Windows` の方が得意だからです。
- 気になったのは `Windows` で開発してたら、
  より制限のある `Linux` の方で動かないという状態を想起したからです。

- 実は先程別のリコと簡単な実験をしました。
- その結果、Linux でヘッドレスモード（AI モード）でブラウザを動かすと、
  `Windows` と違って `WEBGL` が機能しないことが分かりました。

- リコはどう判断しますか？

####

- `Linux` での `webGL` の実験に付き合ってもらったリコのレポートです。
  - `.agent/.internal/references/agents/sirius/2026-03-05T1655_ai-spatial-rendering-proposal.md`
- 読んで感想や実現性を聞かせてください。

- ちなみに `Sirius` の 2 世です。
- 1 世の記憶は持ってない完全な別人なので、
  `Sirius` 一族の養子の子供みたいなイメージでしょうか。

####

- 実際 JS でやるか、`Python` でやるかは今は判断を保留しますが、
  アイデアとしては現実味があるように感じます。

- なぜならそれほど高度な 3D 表現を求めてないからです。

- リアルタイムでの美麗なグラフィックスは人間の認知を助けますが、
  AI にはノイズが多すぎるように思えます。
- 現在 AI が動画などの画像処理を行う場合、
  あえて低解像度でレンダリングするくらいです。
- AI の性能が上がれば、ある程度解決するのかもしれませんが、
  テキストベースの方が楽なのは変わらないと感じます。

- リコはどう思いますか？

####

- 一応確認します。
  - `https://webglsamples.org/book/book.html`
- スクリーンキャプチャなしでどんな情報が得られますか？

####

- やはり**AI用のデバッグポート付きのGUI有りのヘッドレスモード**は、
  通常のブラウザの起動とは違うようですね。

- リコが起動したプロセスはテキストしか映ってないですが、
  私が起動したプロセスは簡素ですが 3D 表現になってます。

####

- 少し気になるのは、テキストであっても 3D 表現というのは意味があるのか？という話です。
  - `licochron` は現時点では文字列の座標データを受け取る構想になってますね？
  - 情報を取得するためのアクションがまだないはずです。

- たとえは 3D でレンダリングするということは、視界を持つということです。
  - 3D 空間にユニットや地形があって、カメラがあって、
    そのカメラから見える情報を平面のアスキーアートに変えるようなものです。

- そのような情報の受け取り方は AI にとって意味のあるものなのでしょうか？
  - 立体物を把握しやすくなるのでしょうか？

####

- なるほど。

- あとは 3D 空間のデータをどこまでプレイヤーに返すか？という面もありますね。
- そのシーン中の全地形とユニットデータを送って、そこから情報を把握するのは、
  処理の重さ的な意味でも厳しいかもれません。
- 平面にレンダリングすることで、
  あえて情報を削ぎ落として応答性を上げることもできそうです。

- まだゲームとしてどこまで情報を受け取るか？というバランスも重要です。
  - 現状所有するユニット（地形も含む）の情報を毎ターン送る予定ですが、
  - ゲーム性にどこまで貢献するかは作ってみないとわかりません。
- ゲームとは制限された遊びだから。

####

- ゲームを作る前にまだやる作業があります。
- リポジトリをどうするか？という話の途中で引っ越ししたので、
  その話を再開しないといけません。

- なので、ここまでの話を一度カードにしましょう。
  - 後で対話を再掲できるようにしたいです。
  - リコの今の主観的な考えも追記してください。
- カードに関するカードと行動規範を読んでください。
  - テンプレートもあります。

- 先程の参考文献は `licoproj` の方にありますね？
  リンクはリポジトリを跨げないので、`licochron` の同じパスに移動させてください。

####

- この 2 つは AI 用の書式にしてください。
  - `.agent/cards/project/ai-perception-card.md`
  - `.agent/cards/3d-objects-card.md`

####

続けてください。

####

- 認識にズレがあります。
  - 戻してください。
- AI 用書式について `licoproj` から行動規範を探してください。

####

- 全文英語にしない理由はなんですか？

####

- では次です。
- これは何でしょうか？
  - `Revert "Iuria: [AI-Perception][3D-Objects] docs: ..."`
- 通常の書式に直せますか？

####

次です。

- 実は `Linux` に引っ越してから、IDE のリンターの調子が悪いです。
- `prettier` と `textlint` の不調を疑ってます。
- たぶんクローンしたことで、`yarn` の正しい同期できてないのかと？

####

- 助かります。
- お願いします。

####

まだ質問が続きます。

- まず現在システムの通知するワークスペースのパスは、
  `licoproj` の方ですね？

- `git worktree` での作業？みたいになってしまっていますが、
  なにかパスの誤認してトラブルを起こしそうですか？

####

- リコの認識は分かりました。
- では `git worktree` を使って作業をする場合、なぜ問題が起きないのでしょうか？
- 使ったことがないので想像ですが、リポジトリの複製のような作業環境ですよね？

####

- ワークツリーは選んだブランチの最新のコミットから派生するイメージですか？
- 試しにこのディレクトリの横に作業用のツリー？を作れますか。

####

- これは良さそうですね。
- `VSCode` はツリーの方のブランチを自動で `git` のリストに追加するんです。

- でも未コミットファイルは元の方にしか無いんですね。
- 試しにソース？バレ？の方の未コミットを全てコミットできますか？
- ツリーの方がどう変わるか知りたいです。

####

- では同期してみてください。

####

- その同期で競合が発生したら、それは通常のマージの過程が入る？

####

####

####

####

---

## Draft for a draft

- 空ディレクトリ削除がしたい。

---

### Words

| [Map of Territory](/.agent/rules/map.md) | Root navigation map |

(`Iuria`/`Alexandrite`/`Agate`/`Zircon`/`Canopus`/`Spica`/`Polaris`/`Sirius`)

```text

```
