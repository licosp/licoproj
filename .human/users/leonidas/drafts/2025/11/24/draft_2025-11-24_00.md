---
ai_visible: true
created: 2025-11-24
part: 0
language: jp
author: leonidas
tag: [draft,scratchpad]
---

# Questions and instructions for the AI.

## Antigravity AI's

### 要約: プロンプト下書きの保存とフォーマット指定
```text
“.agent/.temp/”アイへの指示に使うプロンプトの下書きを保存したい
下書きはには日付時刻、ユーザーが補足情報として記録される
```

### 要約: auto-translate.mdの英訳とプロンプト化
```text
.agent/task/auto-translate.mdを英訳して
さらにプロンプトとして最適なフォーマットとして校正して
```

### 要約: ドラフトファイルの移動とリネーム
```text
“.agent/draft/draft_2025-11-24.md”を”.agent/prompt/”に移動
ついでに最適な名前にリネームして
```

### "gitのコミットメッセージは全て英語で"
```text
gitのコミットメッセージは全て英語で
```

### 要約: draft-prompt.mdの日付フォーマット修正
```text
“.agent/workflows/draft-prompt.md”を編集したい
プロンプト中に出てくる日付のフォーマットから時刻情報を消して
さらにタイムゾーン情報を付与して
```

### 要約: 日付フォーマットの残骸修正依頼
```text
%Y-%m-%d\_%H%M%S
このフォーマットが残っている
全て直して
```

### 要約: draft-prompt.mdのプレースホルダー推測埋め
```text
.agent/workflows/draft-prompt.md
このプロンプト中の”***”を探して推測で埋めて
```

### 要約: draft-prompt.mdの日付へ時間情報付与
```text
“.agent/workflows/draft-prompt.md”を編集したい
プロンプト中に出てくる日付情報全てに時間情報を付与して
```

### "時間情報のフォーマットは”iso8601”形式に変更して"
```text
時間情報のフォーマットは”iso8601”形式に変更して
```

### "空ディレクトリの削除"
```text
空ディレクトリの削除
```

### 要約: .agent/dist/ai_rule 内のファイル削除
```text
.agent/dist/ai_rule
中のファイルだけ削除
```

### "要約: .agent/dist/task/docs の削除"
```text
.agent/dist/task/docs
削除
```

### 要約: .agent/dist/task/git/commit.md のリネーム
```text
.agent/dist/task/git/commit.md
プロンプトの内容あった名前にリネーム
```

### 要約: implement-auto-translation.md の削除
```text
.agent/source
.agent/prompt/implement-auto-translation.md
削除
```

### 要約: commit_and_push.md の移動
```text
.agent/dist/task/git/commit_and_push.md
を
.agent/prompt/
に移動
```

### 要約: multilingual.md の移動
```text
.agent/task/multilingual.md
を
.agent/prompt/
に移動
```

### 要約: .agent 内の空ディレクトリ削除
```text
.agent/
内の空ディレクトリを削除して
```

### "auto-translate.md 英訳して"
```text
.agent/task/auto-translate.md
英訳して
```

### 要約: auto-translate.md の移動
```text
.agent/task/auto-translate.md
を
.agent/prompt
に移動して
```

### 要約: プロンプトファイルの一括リネーム
```text
.agent/prompt
.agent/rules
.agent/workflows
この3つのディレクトリの中のファイルを全てその内容に合ったものにリネームして
```

### 要約: プロンプトディレクトリの清書
```text
.agent/prompt
このディレクトリの中のファイルはエージェント向けのプロンプトです
内容をエージェントの理解に最適な形で清書して
```

### "prompt/commit-and-push.md 実行して"
```text
.agent/prompt/commit-and-push.md
実行して
```

### 要約: MCPサーバーについての質問
```text
MCPサーバーとは何か
このプロジェクトにおすすめのMCPサーバーがあれば教えて
```

### "dockerCLIをインストールしたい"
```text
dockerCLIをインストールしたい
```

### "GitHubのMPCサーバーをAntigravityにインストールしたい"
```text
GitHubのMPCサーバーをAntigravityにインストールしたい
```

### "GitHub CLIをインストールしたい"
```text
GitHub CLIをインストールしたい
```

### "GitHubに新しいIssueを作成したい"
```text
GitHubに新しいIssueを作成したい
```

### "GitHubのIssue作成に必要な情報を定義するファイルを作成したい"
```text
GitHubのIssue作成に必要な情報を定義するファイルを作成したい
```

### "テンプレートをプロジェクトのGitHub用ディレクトリにダウンロードして"
```text
テンプレートをプロジェクトのGitHub用ディレクトリにダウンロードして
```

### 要約: VS Code拡張のエラー解決依頼
```text
github.vscode-pull-request-github
上記をインストールしたけど
"ビュー データを提供できるデータ プロバイダーが登録されていません。"
というエラーが出て動かない
```

### "VS CodeではなくAntigravityで発生したエラーです"
```text
VS CodeではなくAntigravityで発生したエラーです
```

### 要約: ワークフローの清書
```text
.agent/workflows
このディレクトリの中のファイルはエージェントの行動規範を定めるプロンプトが書かれています
内容をエージェントの理解に最適な形で清書してください
```
