---
ai_visible: true
created: 2025-11-25
part: 0
language: jp
author: leonidas
tag: [draft,scratchpad]
---

# Questions and instructions for the AI.

## Antigravity AI's

### 要約: 英訳による変更点の確認依頼
```text
清書したものを英訳して元のファイルとどの程度変わったか教えて
```

### 要約: 言語による精度の違いについての質問
```text
アイへの指示は英語と日本語ではどちらがより精確な返答を返す？
```

### 要約: 言語使用ルールの制定 (思考英語/保存英語/応答日本語)
```text
今後は以下のルールを遵守して
思考は英語で行う
文章として保存するものは英語書くで
文章として保存しない返答などは日本語で行う
```

### 要約: 日本語保存の例外ルール追加
```text
もう一つ
「英文を和訳せよ」などの形で日本語による文章化を指示された場合のみ日本語で文章を保存して
```

### 要約: 未使用gitignore項目の削除依頼
```text
清書の作成時にgitignoreの問題があったね
まずgitignoreの項目の中で現在プロジェクトで使っていないものを削除したい
```

### 要約: 具体的なgitignoreの問題点の質問
```text
清書の作成時にgitignoreの問題について
具体的にgitignoreのどの部分が問題だった？
```

### "アイに”.agent/rules/ ”フォルダへの書き込みを常時許可することはできる？"
```text
アイに”.agent/rules/ ”フォルダへの書き込みを常時許可することはできる？
```

### 要約: 暗黙的な書き込み禁止ディレクトリの調査
```text
他にもにgitignoreに書かれていない形で書き込みが禁止されているディレクトリはどこ？
```

### "では”.agent/”ディレクトリ全体に書き込み許可を与える"
```text
では”.agent/”ディレクトリ全体に書き込み許可を与える
```

### "さらに”.vscode/”ディレクトリ全体に書き込み許可を与える"
```text
さらに”.vscode/”ディレクトリ全体に書き込み許可を与える
```

### "“node_modules/”に対するアイの書き込み権限はある？"
```text
“node_modules/”に対するアイの書き込み権限はある？
```

### "現在”.vscode/”ディレクトリ全体に書き込み許可を与える"
```text
現在”.vscode/”ディレクトリ全体に書き込み許可を与える
```

### "“.vscode/”に対するアイの書き込み権限はある？"
```text
“.vscode/”に対するアイの書き込み権限はある？
```

### 要約: create-prompt-draft.md の移動指示
```text
.agent/workflows/create-prompt-draft.md
を
.agent/prompt
に移動して
```

### 要約: packages-coding-conventions.md の清書と置換
```text
.agent/rules/packages-coding-conventions.md
を削除して
.agent/rules/packages-coding-conventions_cleaned.md
の文字"_cleaned"を削除しリネームして
```

### 要約: agent-code-of-conduct.md の清書と置換
```text
.agent/rules/agent-code-of-conduct.md
を削除して
.agent/rules/agent-code-of-conduct_cleaned.md
の文字"_cleaned"を削除しリネームして
```

### "~/.gemini/GEMINI.md を削除して"
```text
~/.gemini/GEMINI.md
を削除して
```

### "~/.gemini/antigravity/brain/ このディレクトリは何に使われている？"
```text
~/.gemini/antigravity/brain/
このディレクトリは何に使われている？
```

### 要約: brainディレクトリの言語ルール確認と日本語許可
```text
~/.gemini/antigravity/brain/
アイにファイルを書き込み際の言語は何？
英語のみ？
もしそうならこのディレクトリは日本語での書き込みも許可する
```

### "そのルールを .agent/rules/agent-code-of-conduct.md に追記して"
```text
そのルールを
.agent/rules/agent-code-of-conduct.md
に追記して
```

### "現在アイに行動規範を定める全てのファイルを教えて"
```text
現在アイに行動規範を定める全てのファイルを教えて
```

### 要約: .agent内でデフォルト参照されるディレクトリの調査
```text
ディレクトリ.agent/
の中で
geminiがデフォルトで参照するディレクトリを全て教えて
e.g. "rules"
```

### 要約: デフォルト参照先への手動ファイル作成の是非
```text
.agent/ ディレクトリ内のシステムがデフォルトで参照・利用するディレクトリには
アイを介さないファイル作成は控えるべきか？
```

### "workflowは具体的に何が他と違うの？"
```text
現在 prompt と workflows の両方が存在しているけど
workflowsは具体的に何が他と違うの？
```

### 要約: 特殊アノテーション(// turbo)の仕様確認
```text
// turbo や // turbo-all といったアノテーションについて知りたい
他にも特別なアノテーションはある？
```

### "YAMLフロントマターを詳しく"
```text
YAMLフロントマターを詳しく
```

### "ディレクトリ .agent/prompt/ の中身を全て .agent/workflows/ に移動して"
```text
ディレクトリ .agent/prompt/ の中身を全て .agent/workflows/ に移動して
```

### 要約: workflowsファイルのYAMLフロントマター自動補完
```text
.agent/workflows 内のファイルで、
アイが認識できるYAMLフロントマターが書かれていないものを探し
足りてない項目を推測して自動で追記して
```

### 要約: gitコマンドによる.draftディレクトリの反映漏れ調査
```text
gitコマンドの指示を出した場合
ディレクトリ　.agent/.draft/　の中のファイルが反映されない気がする
何か無視設定が指定されてる？
```

### "git の管理対象に追加して"
```text
git の管理対象に追加して
```

### "コミットして"
```text
コミットして
```

### 要約: commitワークフローからpush処理の削除
```text
git操作を行うワークフロー
.agent/workflows/commit-and-push.md
の中からリポジトリをプッシュする作業をなくして
コミットまでで良い
```

### "変更内容に合わせてファイル名もリネームして"
```text
変更内容に合わせてファイル名もリネームして
```

### 要約: プロジェクトのファイル形式に合わせたフォーマッタ提案
```text
このプロジェクトで使っているファイル形式が複数あるけど
それに合ったフォーマッタを提案して
ファイル数が多い形式を優先して表示
```

### "Prettier をインストールして"
```text
Prettier をインストールして
```

### "リポジトリをフェッチしてマージの競合がなけれはプッシュして"
```text
リポジトリをフェッチしてマージの競合がなけれはプッシュして
```

### "今何をしたかトラックできてる？"
```text
今何をしたかトラックできてる？
```

### "新しいディレクトリの作成は認識してる？"
```text
新しいディレクトリの作成は認識してる？
```

### "空ディレクトリの作成は認識できるの？"
```text
空ディレクトリの作成は認識できるの？
```

### 要約: rules/workflowsファイルの和訳計画作成依頼
```text
.agent/rules
.agent/workflows
この2つのディレクトリの中のプロンプトファイルを和訳したい
そのための計画書を作って
```

### "一度翻訳作業を中止して"
```text
一度翻訳作業を中止して
```

### 要約: 翻訳保存先ディレクトリの指定追加
```text
翻訳プロジェクトには
翻訳結果を保存するディレクトリの指定がないので追記したい
```

### 要約: implementation_plan.md の英訳とワークフロー登録
```text
その前に　implementation_plan.md　を正式なワークフローとして
.agent/workflows　に登録していた
けど.agent/workflows　は英語で書かれたファイルだけを保存したいので
implementation_plan.md　を英訳して最適なタイトルを付けた上で
.agent/workflows　登録して
```

### 要約: 古い翻訳計画書(translate-and-cleanup.md)の削除
```text
.agent/workflows/translate-and-cleanup.md
は古い翻訳プロジェクトの計画書なので削除して
```

### "localize-prompts.md は復元して"
```text
.agent/workflows/localize-prompts.md
は復元して
```

### "localize-prompts.md の実行"
```text
.agent/workflows/localize-prompts.md
の実行
```

### "commit.md の実行"
```text
.agent/locales/ja/workflows/commit.md
の実行
```

### 要約: GitHub Pagesのブランチ設定(main vs gh-pages)の違い
```text
GitHub Pages　の　Build and deployment　設定の　Branch　項目は
main と　gh-pagesが選べるけど
これは何が違うの？
```

### "GitHub の gh-pages ブランチを現在使ってないなら削除したい"
```text
GitHub　の　gh-pages ブランチを現在使ってないなら削除したい
アイに作業を頼める？
```

### 要約: README.mdの更新計画とAI管理の明記
```text
README.md　をリポジトリの実態にあった文章に更新したい
その計画書を提案して
あと「このリポジトリの大部分がアイによって編集管理されてる」ことを明言したい
```

### "複数プロジェクトが単一リポジトリとして管理されている点を明記したい"
```text
複数プロジェクトが単一リポジトリとして管理されている点を明記したい
```

### "アイのAIモデルの候補一覧と現在のトークン使用状況を調査して"
```text
アイのAIモデルの候補一覧と現在のトークン使用状況を調査して
```

### 要約: Antigravity固有のモデル解説とトークン推測
```text
一般的な話ではなく
このIDE antigravity の使用できるモデルについての解説を行って
そしてトークン使用状況を推測して
```

### "gemini-cli をインストールしたい"
```text
gemini-cli　をインストールしたい
```

### "インストール作業を任せられる？"
```text
インストール作業を任せられる？
```

### "gemini-cli ってpython用モジュールしかないの？"
```text
gemini-cli　ってpython用モジュールしかないの？
```

### 要約: フィードバックUI(Good/Bad)の意味の質問
```text
アイの返答に対してgoodとbadという反応が送れるUIがあるよね？
これは何の意味があるの？
```

### 要約: commit.mdの英訳と上書き (Turbo使用)
```text
// turbo-all
.agent/locales/ja/workflows/commit.md
この日本語で書かれた指示書を英訳して
.agent/workflows/commit.md
として上書きして
```

### 要約: Turboアノテーションの使用例の質問
```text
アイ向けの指示書を書く場合の
// turbo や // turbo-all といったアノテーションがつけられるけど
その使い方の例を教えて
```

### "// turbo-all ... localize-prompts.md"
```text
// turbo-all
.agent/workflows/localize-prompts.md
```

### 要約: マークダウン指示書での引数渡し(プレースホルダ置換)の再現
```text
アイ向けの指示書(.mdファイル)を書く場合の
特定のパスを置換して実行するようなことはできる
具体的には関数に引数を与えるような形式を再現したい
```

### 要約: localize-prompts.md の和訳と配置
```text
.agent/workflows/localize-prompts.md
この英語で書かれた指示書を和訳して
.agent/locales/ja/workflows/localize-prompts.md
として上書きして
```
